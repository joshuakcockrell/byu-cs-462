'using strict';

var User = require("./user.js");


let user = new User('frank');
let userId = user.id; // save this
users.push({id: user.id, user: user});

let user2 = new User('fasadf');
let userId2 = user.id; // save this
users.push({id: user2.id, user: user2});

getUser(userId).createRumor('My message hi');
getUser(userId).createRumor('My message hi');
getUser(userId).createRumor('My message hi');
getUser(userId).createRumor('My message hi');
getUser(userId).createRumor('My message hi');
getUser(userId).createRumor('My message hi');
getUser(userId).createRumor('My message hi');
getUser(userId).createRumor('My message hi');
getUser(userId).createRumor('My message hi');
getUser(userId).createRumor('My message hi');
getUser(userId).createRumor('My message hi');
getUser(userId).createRumor('My message hi');
getUser(userId).createRumor('My message hi');
getUser(userId).createRumor('My message hi');





















